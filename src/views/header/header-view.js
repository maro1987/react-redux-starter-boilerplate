import React, { Component, PropTypes } from 'react';

export default class HeaderView extends Component {
  static propTypes = {};

  render() {
    return (
      <header>
        HEADER
      </header>
    );
  }
}
